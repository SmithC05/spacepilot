fastapi==0.115.0
uvicorn[standard]==0.30.6
sqlalchemy==2.0.35
pydantic==2.9.2
pydantic-settings==2.5.2
python-dotenv==1.0.1
httpx==0.27.2
# Note: If running backend and mcp in the same Python env, use virtual environments
# (python -m venv .venv) to avoid starlette version conflicts between FastAPI and MCP SDK.
